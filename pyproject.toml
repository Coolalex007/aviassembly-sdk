[build-system]
requires=["setuptools>=68"]
build-backend="setuptools.build_meta"

[project]
name="aviassembly-sdk"
version="0.1.1"
description="SDK for Aviassembly .planedesign files"
requires-python=">=3.11"

[project.optional-dependencies]
test=["pytest>=8"]
